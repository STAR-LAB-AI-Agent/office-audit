# 交付前审查记录

审查日期：2026-09-15。范围为阶段1提交 `d7b0ad5` 之后的本地改动、课程报告和手册。原始材料已另行备份，没有删除真实样例或覆盖用户标注。

## 代码与实现

保留了新增受控样例、规则开关、资源预检与 CI 配置。复查后补充以下修复及回归测试：

| 问题 | 修复 |
| --- | --- |
| 资源阈值接受 NaN、无穷值、布尔值或小数计数 | 只接受正有限值，文件字节数与条目数要求整数 |
| 评测样例 ID 可以带路径；空清单或冲突标签可能误导验收 | 限制 ID 字符和长度，拒绝重复、空集及冲突标签 |
| 未审计对象只检查预期子集 | 声明对象类型时核对完整类型集合 |
| 公式不告空段，却没有明确登记为未审计内容 | 增加 equation 类型，避免嵌套公式重复计数 |
| 纯图片单元格误报为空 | 识别内容承载段落 |
| 禁用规则后仍执行占位符正则，且结果未说明禁用项 | 跳过相应搜索，输出 disabled_rules |
| 不同路径的硬链接可绕过输入输出同文件保护 | 对已存在路径增加文件身份比较 |

没有把这些检查描述为绝对安全。并发修改、正则执行时间、复杂文档资源消耗和依赖库风险仍有边界。

## 课程要求与材料

- 原安全报告偏向项目设计说明，已改写为独立的香港深度伪造视频会议诈骗案例，分别说明事实、分析和建议，引用官方及第一手资料。
- 实验报告改用本机实测性能数据；删除未经验证的硬件、速度和安全保证。
- 真实样例复核纠正了“空单元格必然漏填”“空段必然错误”的结论，保留历史视觉检查与本轮检查的日期区别。
- 宿主记录保留9月11日的 Codex、Nanobot、Claude Code 证据，不把 Antigravity 开发环境运行 CLI 写成第四种宿主验收。
- 手册整理15篇记录与超过5000汉字的总结，未发生的答辩、教师评价不写为事实；日期、个人身份和签名仍需本人核对。

## 本轮验证

Python 3.9 与 3.12 均通过46项测试；15组受控样例通过，文档—规则级 TP=8、FP=0、FN=0、TN=27。12份真实文档回归均无解析错误，输入哈希不变，识别到33个未审计对象。合成指标不代表真实文档整体准确率。

本轮为直接代码和要求双维度复查。独立子代理因运行环境错误未启动，不声称已完成独立双人审查。GitHub CI 在云端针对 Python 3.10、3.11、3.12 矩阵实际执行已全部通过（以最新 main 分支工作流动态状态为准）。

## 提交前仍需核对

1. 用最终代码在课程 Nanobot 环境完成一次新版冒烟测试并保留记录。
2. 检查手册封面、承诺书签名及待核对日期。
3. 录制不超过一分钟的演示，使用合成文档，不展示私人报告。
4. 将审核后的公开文件提交到仓库，检查 CI，并记录最终提交号。

截至本记录形成时，以上事项不能仅因代码测试通过而标为完成。
