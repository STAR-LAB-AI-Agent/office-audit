# 当前验收状态

> 状态日期：2026-09-11。本文是当前验收事实的单一入口；设计说明、实验报告和交付报告保留过程细节，但其中的数量与完成状态如有差异，以本文和对应的原始证据为准。

## 证据层级

本文按证据产生方式区分状态，避免把“存在配置”或“曾经跑过”写成当前验收通过：

| 层级 | 含义 | 本页对应内容 |
| --- | --- | --- |
| A：本轮可复现实跑 | 2026-09-11 在当前工作区执行命令并取得通过结果 | 16/16 unittest、5/5 受控样例 |
| B：本轮仓库事实核验 | 直接读取当前 Git、文件或 manifest；能确认仓库内状态，但不能替代外部服务或行为验收 | 分支、`origin` 配置、MIT `LICENSE`、依赖范围与实际导入版本、12 条候选状态 |
| C：既有端到端记录 | [运行时兼容记录](runtime-compatibility.md)记载 2026-09-11 的实际宿主调用，本轮文档更新未重跑 | Codex、Nanobot、Claude Code |
| D：历史工程记录 | 过去运行留下的可追溯结果，本轮未重跑 | 12 份仓库外候选的下载、容器校验和离线解析 |

低层级编号不代表证据无效，而是适用范围不同。任何层级都不能越过其“不能据此推断”的边界；未运行、未核验或无法从当前证据确认的事项统一保留为未完成。

## 仓库锚点

| 项目 | 当前事实 | 证据边界 |
| --- | --- | --- |
| 分支 | `main` | 当前分支为 `main`；具体提交以 Git 为准 |
| Git 远程 | `origin` = <https://github.com/STAR-LAB-AI-Agent/office-audit.git> | 来自本地 Git remote 配置；只证明远程地址已配置，不代替仓库可见性、分支保护、Release 或最终 Git 历史检查 |
| 项目许可证 | [MIT](../LICENSE) | 仓库根目录存在 MIT `LICENSE`；只覆盖该许可证声明适用的项目代码和文档，不自动授予第三方样例的再分发权 |
| Python 运行时 | 本轮 `python --version` 为 3.9.0 | 这是当前 shell 的实跑环境，不是课程绑定版本；[历史性能记录](performance-baseline.md)另有 Python 3.12.13 结果。课程建议使用 3.10–3.12，但本轮没有新增这些版本的测试 |
| Python 依赖 | `requirements.txt` 约束 `python-docx>=1.2,<2` | 这是允许安装的版本范围，不是锁定版本；2026-09-11 本轮环境实际导入 `python-docx 1.2.0` |

## 当前验收结果

| 验收面 | 状态 | 2026-09-11 证据 | 不能据此推断 |
| --- | --- | --- | --- |
| 单元测试 | **16/16 通过** | `python -m unittest discover -s tests -v`；Python 3.9.0；16 项均为 `ok` | 不代表所有真实 Word 排版、宿主版本或操作系统组合都已覆盖 |
| 受控样例 | **5/5 通过** | `python scripts/evaluate_controlled_cases.py --format terminal`；临时生成且不保留，网络禁用 | 不代表真实公开数据的规则准确率、隐私合规或再分发许可已通过 |
| Codex | **已有端到端通过记录** | [运行时兼容记录](runtime-compatibility.md)记载 2026-09-11 的 Skill 发现、三类路由、报告生成、覆盖拒绝和输入哈希不变 | 本轮文档更新未重新执行独立 Codex 宿主验收，也不代表未来版本自动兼容 |
| Nanobot | **已有端到端通过记录** | [运行时兼容记录](runtime-compatibility.md)记载 2026-09-11 的三类报告结果和原文哈希边界 | 本轮未重新执行 Nanobot；记录不等于所有规则准确率达到最终标准 |
| Claude Code | **已有端到端通过记录** | [运行时兼容记录](runtime-compatibility.md)记载 2026-09-11 的三类报告结果和原文哈希边界 | 本轮未重新执行 Claude Code；记录不等于所有规则准确率达到最终标准 |
| Antigravity | **未验证** | 当前没有实际宿主调用记录 | 不得仅凭目录设计或脚本可运行宣称兼容通过 |

## 公开候选状态

2026-09-11 直接读取 `data/public-samples.manifest.json` 得到：

- 候选记录：12；
- 发布候选状态：12/12 为 `pending`；
- 人工 PII 核验：12/12 为 `not_started`；
- 文件级再分发状态：12/12 为 `UNVERIFIED`。

已有记录显示 12 份仓库外候选曾完成下载、容器校验和离线解析评估。这只证明当时的工程链路可运行，不会把候选提升为可公开样例：首轮文本风险筛查不是正式 PII 核验，数据集元数据许可不等于单个 `.docx` 的文件级再分发许可，原始候选仍不得进入公开交付物。

## 阶段性结论

当前可以确认的是：项目已有 MIT 许可证和 GitHub `origin`，本轮 16/16 单元测试与 5/5 受控样例通过，Codex、Nanobot、Claude Code 存在端到端记录。当前仍不能确认的是：Antigravity 兼容性、12 条候选的正式 PII 与文件级再分发结论、最终视频、最终课程报告、公开发布状态和最终 Git 历史清理。

更新这些状态时，应先更新原始证据，再同步本文；其他文档尽量引用本文，不另设一套“当前完成度”。
