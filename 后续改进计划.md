# Office Audit 后续改进与交付计划

> 2026-09-15更新：阶段2/3已有Antigravity实现并进入交付审查；本轮46项单元测试、15组受控样例通过，12份真实回归无解析错误且哈希不变。原计划中的待办是9月14日快照，当前事实以docs/validation-status.md为准。按用户反馈准备9月17日提交、18日答辩。剩余重点为宿主新版复测、手册日期与个人信息确认、提交文件及CI核验，不再扩展功能范围。

更新时间：2026-09-14。本文用于安排后续工作，不代表下列任务已经完成。验收事实统一记录在 [当前验收状态](docs/validation-status.md)。

## 一、当前进度与边界

本次核对本地分支为 `main`，HEAD 为 `d7b0ad5`，工作区在新增本文前干净。未联网核对远程最新状态，也没有重新运行测试。

| 项目 | 已有证据 | 后续缺口 |
| --- | --- | --- |
| 阶段0：文档与状态整理 | 已有提交 `2dcc5f3` | 后续改动继续同步状态 |
| 阶段1：减少排版误报 | 已有提交 `d7b0ad5`；9月12日记录为25项单元测试、5组受控样例通过 | 不等于真实文档准确率已经达标 |
| 真实文档回归 | 12份公开候选完成阶段1前后对比，输入哈希不变，33个未审计对象数量不变 | 需要针对剩余问题复核；缺少逐项质量标签 |
| 宿主测试 | Codex、Nanobot、Claude Code 有9月11日运行记录 | 阶段1及后续版本需要重新测试；Antigravity未验证 |
| 阶段2：评测与自动测试 | 尚未落地 | 下一项主要工作 |
| 课程交付 | 已有报告、演示脚本等材料 | 最终内容、视频、手册和提交清单仍需验收 |

项目继续面向通用 `.docx`，不绑定某种学校报告模板。必需章节由用户提供；无标准时不擅自判定缺章。审计只读，不自动修复原文；图片、公式等未审计对象必须逐个说明位置与限制。此轮不扩展到 Excel、PPT、OCR，也不将 PII 检测包装成已经完成的能力。

## 二、执行前：文档与接口核对

此步骤是每轮恢复工作的起点，不是重新实施已完成的阶段0。

- [ ] 检查 Git 状态与差异，保留用户修改；确认本轮基线提交。
- [ ] 阅读当前验收状态、阶段1评估、受控样例说明和运行时兼容记录。
- [ ] 先运行现有测试，保存实际环境与结果，不能沿用旧计数冒充新结果。

已核对、可复用的入口：

| 入口 | 用途与来源 |
| --- | --- |
| `audit_document(input_path, *, mode, required_sections, rules)` | 统一审计内核；见 [audit_docx.py](scripts/audit_docx.py) |
| `evaluate(manifest_path)`、`_check_case(case, root)`、`BUILDERS` | 受控样例生成与比对；见 [evaluate_controlled_cases.py](scripts/evaluate_controlled_cases.py) |
| `--input`、`--mode`、`--request`、`--rules`、`--required-sections` | 已有 CLI 参数；不另造宿主专用审计接口 |
| `--sample-root`、`--baseline` | 真实样例基线对比；见 [compare_rule_baseline.py](scripts/compare_rule_baseline.py) |

新配置键、资源限制参数和 CI 工作流均是待实现事项，不能提前写进使用说明作为现成功能。基线工具会执行指定提交中的代码，只能选取可信的本地 Git 提交。

## 三、阶段2：补齐评测和自动测试（优先）

### 2.1 扩充受控样例

沿用现有临时生成 DOCX 的方式，将5组扩展到约12–15组，具体数量服从覆盖需求，不为凑数重复样例。

- 保留现有正常文档、标题跳级、缺章、字段与格式、绘图对象样例。
- 补充正常标题/列表/引用/表格排版，验证不应产生的格式告警。
- 补充单空段、连续空段、跨表格留白及图片/公式/分页承载段。
- 补充视觉标题候选与真正 Heading 的区别，确认候选不会自动满足必需章节。
- 补充表格列独立格式基线、真实格式异常、无多数格式等边界。
- 补充文件损坏、路径不存在、非法规则及输出覆盖拒绝；已有单元测试覆盖的内容可复用，不要求全部塞入同一个样例生成器。

参考：[受控样例说明](docs/controlled-cases.md)、[标签清单](fixtures/controlled-cases.json)、[阶段1测试](tests/test_phase1_rules.py)。

### 2.2 明确误报与漏报口径

- 为样例声明预期命中和明确不应命中的规则，不能只验证“应该出现的出现了”。
- 受控评测先按“文档—规则是否命中”统计 TP、FP、FN，注明不衡量同一规则下每个位置的完整性；数量、严重级别与定位另做断言。
- 明确评测规则集合。信息提示、未审计对象和解析错误分别检查，不混入缺陷准确率。
- Precision = TP / (TP + FP)，Recall = TP / (TP + FN)；分母为零时标记不适用。
- 给评测器本身加入故意多报、漏报的测试，证明指标与失败退出码确实有效。
- 合成样例指标单独发布，不能称为真实文档整体准确率。

### 2.3 真实文档复核

优先复核阶段1仍有明显格式提示的02、05、06、11号文档，并检查08、10号尾部空白及之前告警大量减少的样例，防止漏报。先利用已有页面预览，再对缺失或不清晰的页面重新渲染。

记录匿名编号、规则、定位、视觉判断及理由；区分“确定缺陷”“正常排版”“无法确定”。无法确定的对象不强行计入准确率。必要时只请用户确认有歧义的少量项目，不再要求逐篇检查全部告警。

保留12份真实文档作为回归基础。只有发现文档类型或边界缺口时再补公开样例，不以单纯增加数量代替覆盖；私人报告仍只在本地使用。原文不公开，因此文件再分发核验不是本地评测的前置条件。

### 2.4 GitHub 自动测试

新增最小权限工作流，目标覆盖 Python 3.10、3.11、3.12，执行单元测试和受控评测。实施时核对 GitHub 官方 Actions 文档及所用版本，不在计划中预设未经核验的 Action 版本。

CI 不需要模型密钥，不下载私人文档，不上传原始审计报告。工作流文件存在不等于 CI 通过，必须查看对应提交的真实运行结果。

验收标准：全部现有和新增测试通过；评测器能识别多报与漏报；12份真实文档无非预期解析退化、输入哈希不变；自动测试取得可追溯结果；文档说明指标适用范围。若发现规则缺陷，先补复现用例，再小范围修复。

## 四、阶段3：规则配置与运行边界

在阶段2提供可靠回归后实施，交付时间紧时可缩小范围。

- [ ] 复用现有 `--rules` 和 `--required-sections`，梳理已有配置及校验缺口，再补必要的规则开关、严重级别或阈值配置。
- [ ] 对未知键、非法类型和无效正则明确报错，避免静默忽略；提供通用示例，不写死学校章节。
- [ ] 优先补单文件大小、ZIP解压后体积及条目数等资源检查；具体默认值经代表性文档实测后确定。
- [ ] 遇到限制明确输出错误或未完成范围，不能把部分扫描描述为完整审计。
- [ ] 目录批处理仅在单文件边界稳定且确有需要时实现，禁止默认扫描整个磁盘或递归访问无关目录。

参考：[审计内核](scripts/audit_docx.py)、[设计说明](docs/phase1-design.md)、[Skill入口](SKILL.md)。

验收标准：默认行为有回归保护；无效配置与超限文件可复现地失败；原文不变；三种宿主仍使用同一内核。规则变化同步说明兼容影响，不偷偷改变结果含义。

## 五、阶段4：宿主复测与最终交付

### 4.1 在同一提交上复测宿主

助手负责准备固定合成样例、运行 Codex 可用的真实宿主测试并汇总证据；用户按简短指南运行 Nanobot 与 Claude Code。无法启动的宿主明确标记未运行。

每个宿主至少检查：完整审计、结构审计、字段与格式审计、输出覆盖拒绝、输入哈希不变。另抽查一份本地真实文档，核对报告定位是否足够清楚。

记录提交号、宿主版本、日期、提示词、模式、结果路径与哈希。比较稳定的规则结果，忽略耗时等运行差异。直接执行 Python 脚本不能替代 Skill 发现和真实调用证据。

参考：[运行时兼容记录](docs/runtime-compatibility.md)、[Nanobot指南](docs/nanobot-test-guide.md)。Antigravity不作为当前必做门槛。

### 4.2 性能与低上下文证据

复跑 [性能基线](docs/performance-baseline.md)中的命令，记录环境和重复次数，补充不同缺陷密度。区分字符压缩比与真实 token 数；审计脚本模型调用为零，不代表宿主理解请求不消耗 token。不将少量重复的最大耗时称为严格 P95。

### 4.3 课程材料

- [ ] 更新实验报告、开源代码报告和 AI 安全案例报告，删除过时测试数量与能力宣称。
- [ ] 按 [演示脚本](docs/demo-script.md)录制不超过1分钟的视频，只展示安全合成文档。
- [ ] 检查实习手册15篇记录，每篇不少于400字，从“我”的角度写实际工作、问题和解决过程。
- [ ] 核对第8天草稿与正式手册状态：此前草稿因 Word 渲染失败未写回。先备份、确认目标内容，再渲染检查后写回，不能将草稿存在等同于完成。
- [ ] 每个实际工作日按固定流程补记录；历史内容依据真实证据整理，不伪造日期或未来成果。同一天继续工作优先补充当日记录。
- [ ] 最终总结按课程文件核对字数；此前材料有4000与5000字差异，暂按不少于5000字准备，最终以老师确认的要求为准。
- [ ] 向老师确认最终提交和答辩日期；此前通知有冲突，不依赖某一个旧日期排到最后一天。

### 4.4 公开提交检查

检查待提交文件及历史中是否含私人原文、报告正文、密钥或本机敏感路径；`.gitignore` 不能清除已跟踪文件或历史记录。保留 MIT 与 `Wh1t3zZ114` 的正确署名。发现历史敏感内容时先报告具体范围，不擅自重写公共历史或强推。

以干净检出的仓库复现安装与测试，检查 README 链接、三种入口和示例命令。完成后按授权提交、推送，核对远程提交与 CI；有需要再建立版本标签或 Release，不把它们当作已完成事项。

## 六、推荐顺序与完成判据

1. 先完成阶段2的受控评测和自动测试，同时复核高风险真实样例。
2. 依据复核结果决定阶段3的最小修改范围；不在提交前大规模重构。
3. 冻结候选提交，完成三种宿主复测，再更新报告和录视频。
4. 手册与最终材料并行整理，最后统一做干净环境复现及公开文件检查。

时间紧时，优先确保“可安装、可调用、结果可定位、原文不变、已知限制写清楚、课程材料齐全”。扩展格式、OCR、自动修复、大规模下载数据及额外宿主验证可以后置。

每阶段交接记录：本轮改了什么、基线与新提交、运行了哪些命令、实际结果、未运行项、剩余缺陷及下一步。测试通过只证明已覆盖的场景，不使用“完全通用”“零误报”等无法证明的表述。
